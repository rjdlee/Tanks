{
  "name": "tanks",
  "version": "0.1.0",
  "scripts": {
    "test": "jest",
    "watch": "jest --watchAll"
  },
  "devDependencies": {
    "express": "^4.14.0",
    "jest": "^20.0.4",
    "socket.io": "^1.3.5"
  }
}
